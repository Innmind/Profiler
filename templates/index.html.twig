<!DOCTYPE html>
<html>
    <head>
        <title>Profiler</title>
        <meta charset="UTF-8">
        <style>
            body {
                margin: 0;
            }

            header {
                background-color: rgb(48, 48, 48);
                display: flex;
                height: 60px;
                padding: 0 10px;
            }

            header img {
                height: 50px;
                padding: 5px 0;
            }

            main {
                margin: 35px;
            }

            main ul {
                border: solid;
                border-width: 1px;
                border-color: rgba(48, 48, 48, 0.2);
                margin: 0;
                padding: 0;list-style: none;
                border-radius: 4px;
            }

            main ul li a {
                color: rgb(52,140,255);
                display: block;
                padding: 10px;
                text-decoration: none;
            }

            main ul li.empty {
                padding: 10px;
            }

            main ul li.succeeded {
                background-color: rgba(104, 255, 101, 0.3);
            }

            main ul li.succeeded:hover {
                background-color: rgba(104, 255, 101, 0.6);
            }

            main ul li.failed {
                background-color: rgba(255, 79, 86, 0.3);
            }

            main ul li.failed:hover {
                background-color: rgba(255, 79, 86, 0.6);
            }

            main ul li.pending {
                background-color: rgba(255, 179, 48, 0.3);
            }

            main ul li.pending:hover {
                background-color: rgba(255, 179, 48, 0.6);
            }

            main ul li + li {
                border-top: 1px solid rgba(48, 48, 48, 0.2);
            }
        </style>
    </head>
    <body>
        <header>
            <a href="index.html">
                <img src="logo.svg" alt="home">
            </a>
        </header>
        <main>
            <ul>
                {% for profile in profiles %}
                    <li class="{% if profile.closed %}{{ profile.status }}{% else %}pending{% endif %}">
                        <a href="/profile/{{ profile.identity }}">{{profile}}</a>
                    </li>
                {% else %}
                    <li class="empty">No profile found</li>
                {% endfor %}
            </ul>
        </main>
    </body>
</html>
